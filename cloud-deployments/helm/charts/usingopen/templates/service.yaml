apiVersion: v1
kind: Service
metadata:
  name: {{ include "usingopen.fullname" . }}
  labels:
    {{- include "usingopen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "usingopen.selectorLabels" . | nindent 4 }}
