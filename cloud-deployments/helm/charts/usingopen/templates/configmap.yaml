apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "usingopen.labels" . | nindent 4 }}
  name: {{ include "usingopen.fullname" . }}-config
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}